extends layout

block content
  h1= title
  - var results = [{"BoatType":"Sail powered boats","BoatsCost":5000,"Capacity":420,"Hull":"V-Shaped Hulls"},{"BoatType":"powered boats","BoatsCost":4000,"Capacity":380,"Hull":"Round-bottomed hulls"},{"BoatType":"Motorboats","BoatsCost":3600,"Capacity":3500,"Hull":"Pontoon hulls"}];
  div(class="container-fluid")
    div(class="row")
    each item in results
      div(class="col-sm-2" width="225px") Boat Type:#{item.BoatType}, Boats Cost:#{item.BoatsCost}, Capacity:#{item.Capacity}, Hull:#{item.Hull}
  br
  div(class="container-fluid")
    div(class="row")
    - for(var i=0;i<results.length;i++) {
        div(class="col-sm-2" width="215px" onClick=`buy(${i+1})`) #{results[i].BoatType}
    -}
    script. 
      function buy(indexNum) {
        alert(`You purchased item ${indexNum}`);
      }
